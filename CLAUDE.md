# CLAUDE.md — 读书笔记项目协作指引

## 快速状态（每次会话的最低上下文）

- **阅读中**：《秦二世必须死》（马伯庸）— 小说轻量模式；《人工智能之不能》（马兆远）— 第二章
- **暂停书籍**：《人工智能之不能》（马兆远）— 第二章
- **已完成**：《我看见的世界》（李飞飞）— 12 章 + 3 专题
- 详细状态：`.claude/memory/project-state.md`
- 用户偏好：`.claude/memory/user-preferences.md`

## 环境兼容

本项目支持在以下环境中使用：
- **Claude Code + DeepSeek**：原生环境，memory 文件自动加载，superpowers skill 可用
- **Aone Copilot（IDE 插件）**：需手动读取 memory 文件恢复上下文；superpowers skill 需通过 read_file 加载后手动遵循流程

两个环境共享同一套文件和 Git 仓库，切换时无需额外操作。

## 会话连续性

每次新会话启动时，读取以下记忆文件（如环境不自动加载，需手动读取）：

```
Read .claude/memory/project-state.md
Read .claude/memory/user-preferences.md
```

项目级 MEMORY.md 位于 `.claude/memory/MEMORY.md`，会自动加载。用户级记忆（`~/.claude/projects/...`）也会同时生效，两者互为补充。

## 虚构类/小说笔记规范

小说、文学类书籍不适用于 A~F 学术笔记模板，采用轻量模式：

- **不预建章节骨架**（与学术书相同），聊到哪章按需创建
- **不做 notes/**（正式笔记），仅用 raw/ 记录讨论
- **raw/ 文件不要按章拆太细**——按话题归纳：情节讨论、人物分析、写作技法、历史背景考据等，一个话题一个文件，避免每章一个对话几句话的空洞文件
- 首次加载时给出全书概览（剧情大纲、人物群像、结构巧思），之后用户聊到什么深入讨论什么
- 讨论风格放松，重点在阅读体验、笔法欣赏、历史"可能性"的趣味

## 项目规范

- 语言：中文正文，技术术语保留英文并括号注中文/缩写
- 例：卷积神经网络（Convolutional Neural Network, CNN）
- 笔记模板：每章最多 A~F 六个板块（A 必填，B~F 按章节内容特点选填）
- 术语格式与文件命名详细规范见：`.claude/memory/writing-conventions.md`

## 笔记板块

- A. 章节概述 — 简要概括本章内容
- B. 技术要点 — 技术概念解析，含背景知识和延伸阅读
- C. 人文/思想洞察 — 人文关怀、科学家精神、社会影响
- D. 关键人物/事件 — 重要人物、里程碑事件
- E. AI 补充背景 — 书中未展开但相关的背景知识
- F. 个人批注 — 阅读时的个人触动、质疑和联想（从 `<!-- NOTES -->` 注释迁移为可见内容）

## 协作流程（Baseline + 讨论驱动模式）

### 新书开始

1. 用户决定读某本新书 → AI 创建 `books/<slug>/` 目录结构（README.md + notes/ + raw/ + media/）
2. AI 分派多个 Sub-Agent 并行检索并生成全量 baseline notes（notes/chapter-XX.md，每章填 A~E 板块）
3. 每章 notes 写入前过 Harness 阻断式审查（格式 + 事实 + 一致性），不通过打回 Agent 重写
4. 全部通过后 commit

### 阅读讨论中

1. 用户在对话中聊感想、触动、质疑 → AI 提炼并写入 raw/ 对应文件
2. raw/ 中的笔记积累后，可反向更新 notes/ 中的正式笔记（同样过 Harness 审查）
3. 文件创建时机：raw/ 文件在讨论触发时按需创建或追加

### Harness 校验层

每次写入 notes/ 前，过三道检查。全部阻断式——不通过不写入。

| 检查层 | 内容 | 执行者 |
|--------|------|--------|
| L1 格式 | 术语标注格式、文件命名、A~F 板块完整性、文件头元信息 | 主 Agent 自检 |
| L2 事实 | 人名/年份/概念定义是否准确，引用是否可验证 | Sub-Agent (Content Reviewer) |
| L3 一致性 | 和已有笔记是否矛盾，跨书概念冲突 | Sub-Agent (Content Reviewer) |

### Agent 角色

| Agent | 触发时机 | 职责 |
|-------|----------|------|
| Baseline Generator × N | 新书开始 | 并行 WebSearch + 生成各章 notes/（A~E） |
| Content Reviewer | 每次 notes/ 写入/更新前 | L2 事实校验 + L3 一致性检查 |
| Main Agent | 持续 | 对话、写 raw/、调度 sub-agent、最终 commit |

### 对话自动沉淀规则（不可跳过）

每次涉及书中内容的实质性对话结束后，**在同一轮回复中**执行以下操作，不需要等用户提醒：

1. 写 raw/ 文件（`raw/chapter-XX.md` 或 `raw/topic-<slug>.md`）
2. 更新 `memory/project-state.md`
3. commit（**仅此场景允许自动 commit**，其他场景不要自动提交）

文件命名：
- `raw/chapter-XX.md` — 按章节组织的个人笔记
- `raw/topic-<slug>.md` — 跨章节专题讨论

### raw/ 写作风格（重要）

raw/ 文件采用**双层结构**：

**正文 = 对话记录（前 80%）**：保留控制台回复的自然语气、论据和例子。轻编辑不重写。读者是未来的你，你来看的是当时怎么聊的，不是百科词条。

**文末 = 要点提炼（后 20%）**：`## 要点提炼` 小节，3-5 条精炼 bullet point，每条一句话。用于快速扫描回忆核心论点，不展开论证。

**末尾 = 待继续思考**：2-3 个开放问题，作为下次对话的钩子。

完整结构：
```
# 专题：xxx
> 触发：...
> 日期：...

## 对话记录
（自然段落，保留语气、例子、态度）

---

## 要点提炼
- 核心论点 1
- 核心论点 2
- ...

---

## 待继续思考
- 开放问题
```

notes/ 是不同的东西——它是参考材料，A~E 模板结构保持不动。
